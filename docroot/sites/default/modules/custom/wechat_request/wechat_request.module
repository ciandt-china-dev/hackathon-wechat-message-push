<?php
/**
 * @file
 * Deal request wechat info.
 */

/**
 * Implements hook_menu().
 */
function wechat_request_menu() {
  $items = array();
  $items['wechat/requesturl'] = array(
    'page callback' => '_wechat_request_info_url',
    'file' => 'includes/wechat_request.menu.inc',
    'type' => MENU_CALLBACK,
    'access callback' => TRUE,
  );
  return $items;
}
